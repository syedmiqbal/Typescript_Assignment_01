const student : {name: string, roll_number: number, teacher: string, course: string, batch: string, quarter_fee: string} = {
    name : "Iqbal",
    roll_number : 786,
    teacher: "Sir Hamza",
    course: "TypeScript",
    batch: "Batch 51-2023",
    quarter_fee: "PKR 4500"
};

console.log(`Our student named ${student.name} having Roll number ${student.roll_number} is attending ${student.course} course`);
